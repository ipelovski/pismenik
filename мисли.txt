език за програмиране като текстово представяне на структура от данни - дърво за изпълнение
първо::второ
получател::съобщение
среда::определител
среда::(определител::стойност)
код => среда::(определител::((среда::определител)::съобщение))
дърво => (среда::определител)::съобщение =>  (стойност) <<среда::(определител::стойност)>>

предикати: "е" определен обект; "(държи се) като" даден обект

съдържание на съобщение => вектор, двойка, запис, атом
атом => число, низ, ключ, указател към обект
определител = ключ ползван за текущата среда

двойка = определител: стойност или стойност :: стойност
вектор = (,) или (,стойност) или (,стойност,) или (,стойност, стойност)
запис = (:) или (:двойка) или (:двойка,) или (:двойка, двойка)
групиране = (израз)
ключ = 'думи
низ = "думи"
изпращане на съобщение = обект ! съобщение;
празно = ( )
функция!( ); <=> функция( ) -> "!;" се подразбира
функция!1; функция(,1,2) функция(:събери: 1, с: 2) функция(,'събери, 1, 2)
@определител = достъп до състоянието на обекта
@!съобщение; = изпращане на съобщение до текущия обект
свързан списък = стойност :: стойност :: стойност :: стойност :: ()

pattern = procedure -> vector or '==>> (can't process, run next)
procedure => handle input => nothing or a list of values (values to be bound to the input parameters) => if list call the body of the procedure otherwise check the next

object!message title(1); служебни заглавия? "вектор започващ с атом 'действие' и дължина 5"
(:: стойност) => (: '': стойност); '' -> празен ключ

обект! +състояние: ... +поведение -> нов обект създаден от стария
